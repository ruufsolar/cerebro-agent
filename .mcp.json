{
  "mcpServers": {
    "ruuf-agents": {
      "type": "http",
      "url": "${RUUF_AGENTS_MCP_URL:-https://agents.ruuf.solar/mcp}",
      "headers": {
        "Authorization": "Bearer ${RUUF_AGENTS_TOKEN}"
      }
    }
  }
}
