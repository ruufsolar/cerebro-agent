{
  "mcpServers": {
    "ruuf-agents": {
      "type": "http",
      "url": "${RUUF_AGENTS_MCP_URL:-https://ruuf-prs.centralus.cloudapp.azure.com/mcp}",
      "headers": {
        "Authorization": "Bearer ${RUUF_AGENTS_TOKEN}"
      }
    }
  }
}
